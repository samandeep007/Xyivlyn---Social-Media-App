# Random Social Discovery Platform

Phase 06 development monorepo.

## Current status
Chunk 01: repository foundation only.

## Workspace
- `apps/mobile` — React Native / Expo client (created in Chunk 02)
- `apps/api` — NestJS backend (created in Chunk 03)
- `packages/design-tokens` — shared visual constants
- `packages/types` — shared TypeScript contracts
- `docs` — architecture decisions and engineering documentation
